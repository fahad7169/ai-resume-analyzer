export const meta = ()=>([
    {title:'Resumind | Auth'},
    {name:'description',content:'Login into your account'},
])

const Auth = () => {
  return (
    <div>auth</div>
  )
}

export default Auth